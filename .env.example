# ===========================================
# SMTP Settings (для отправки email)
# ===========================================
# Хост SMTP сервера (например, smtp.mail.ru, smtp.gmail.com)
SMTP_HOST=smtp.mail.ru

# Порт SMTP (обычно 587 для TLS или 465 для SSL)
SMTP_PORT=587

# Использовать SSL/TLS (true для порта 465, false для 587)
SMTP_SECURE=false

# Email отправителя (ваша почта)
SMTP_USER=your-email@mail.ru

# Пароль от почты (или app-специфичный пароль)
SMTP_PASS=your-password

# ===========================================
# CRM API Settings (опционально)
# ===========================================
# URL вашей CRM системы
CRM_API_URL=https://your-crm-api.com

# API ключ для доступа к CRM
CRM_API_KEY=your-crm-api-key

# ===========================================
# MacDent CRM Settings
# ===========================================
# API ключ (access_token) для MacDent CRM
# Получите его на https://docs-developer.macdent.kz
MACDENT_ACCESS_TOKEN=your-macdent-access-token

# ===========================================
# Примечания:
# ===========================================
# 1. Для Mail.ru используйте app-специфичный пароль
# 2. Для Gmail включите "Less secure app access" или используйте App Password
# 3. CRM интеграция опциональна - приложение работает и без неё
# 4. MacDent интеграция: после прохождения теста заявка автоматически отправляется в MacDent CRM
# 5. В Netlify эти переменные нужно добавить в Site settings > Environment variables
